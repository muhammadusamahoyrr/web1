import CourtroomLoader from '@/components/courtroom/CourtroomLoader';

export const metadata = {
  title: 'Courtroom Session — Attorney.AI',
  description: 'AI-powered legal courtroom simulation environment',
};

export default function CourtroomSessionPage() {
  return <CourtroomLoader />;
}
